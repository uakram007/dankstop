{% comment %}
	GEMPAGE BUILDER (https://apps.shopify.com/gempage)

	You SHOULD NOT modify source code in this page because
	It is automatically generated from GEMPAGE BUILDER
	Try to edit page with the live editor.
{% endcomment %}
<!--GEM_HEADER-->
<link data-instant-track rel="stylesheet" type="text/css" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.6.3/css/font-awesome.min.css" class="gf-style" />
<link data-instant-track rel="stylesheet" type="text/css" href="https://d1um8515vdn9kb.cloudfront.net/files/vendor.css?refresh=1" class="gf-style" />
<link data-instant-track rel="stylesheet" type="text/css" href="{{ 'gem-article-555454136394.css' | asset_url }}" class="gf_page_style">
<!--GEM_HEADER_END-->
{%- comment -%}
<!-- Article JSON Schema =============== -->
{%- endcomment -%}<div class="article-template">
  <div class="container">
    {%- include 'breadcrumb' -%}
    
    <div class="page-header"><h2>
        {{- blog.title -}}
      </h2></div>
    
    <div class="halo-blog-content article-content">
      <div class="row {{ blog.template_suffix }}">
        {%- section 'sidebar-blog' -%}
        {%- comment -%}
  Comments will not appear right after they are submitted, either to be checked for spam
  or to await moderation. When a comment is submitted, the browser is redirected to a page
  that includes the new comment id in its URL.
    Example: http://shopname.myshopify.com/blogs/news/2022072-my-post?comment=3721372  More information:
    - http://docs.shopify.com/themes/liquid-variables/comment
{%- endcomment -%}{%- comment -%}
  We're going to reference the number of comments below, so let's hold it in a variable.
{%- endcomment -%}{%- assign number_of_comments = article.comments_count -%}{%- comment -%}
  If a comment was just submitted and it has no blank fields, we have an extra comment to count.
{%- endcomment -%}{%- if comment and comment.created_at -%}
  {%- assign number_of_comments = article.comments_count | plus: 1 -%}
{%- endif -%}<div class="blog-detail shopify-section col-12 col-xl-9 col-main">
  <h1 class="article-title">{{- article.title -}}</h1>
  
  <div class="blog_author">  
    {%- capture date -%}
    	<time datetime='{{ article.published_at | date: '%Y-%m-%d' }}'>{{ article.published_at | date: '%b %d, %Y' }}</time>
    {%- endcapture -%}
    
    {%- capture author -%}by {{ article.author }}{%- endcapture -%}
    
    {%- if section.settings.blog_show_author -%}
    <span class="article__author">
      {{ author }}
    </span>
    {%- endif %}    {%- if section.settings.blog_show_date -%}
    <span class="article__date">
      on {{ date }}
    </span>
    {%- endif %}    {%- if blog.comments_enabled? -%}
    <a class="blog-comment" href="{{ article.url }}#comments"{% if settings.enable_multilang %} data-translate="blogs.comments.comments_with_count|count:{{ article.comments_count }}"{%endif%}>
      {{ 'blogs.comments.comments_with_count' | t: count: article.comments_count }}
    </a>
    {%- endif -%}  
  </div>
  
  <div class="article-details">
    {%- if settings.enable_multilang and article.content contains '[lang2]' -%}
    <div class="rte lang1">
      
<!--Gem_Page_Main_Editor--><div class="clearfix"></div><div class="gryffeditor"></div><div id="divContentBk"></div><!--End_Gem_Page_Main_Editor-->
    </div>
    <div class="rte lang2">
      
<!--Gem_Page_Main_Editor--><div class="clearfix"></div><div class="gryffeditor"></div><div id="divContentBk"></div><!--End_Gem_Page_Main_Editor-->
    </div>
    {%- else -%}
    <div class="rte">
      
<!--Gem_Page_Main_Editor--><div class="clearfix"></div><div class="gryffeditor"></div><div id="divContentBk"></div><!--End_Gem_Page_Main_Editor-->
    </div>
    {%- endif -%}
  </div>  {%- include 'blog-detail-gallery_lookbook' -%}
  
  <p class="clearfix"></p>
  
  <div class="tags-share">
    <ul class="user-tags">
      {%- include 'tags-article' -%}
    </ul>    {%- include 'share' -%}
  </div>  {%- if blog.next_article or blog.previous_article -%}
  <div class="action-btn">
    
    {%- if blog.previous_article -%}
    <div class="btn-left">
      <i class="fa fa-angle-left" aria-hidden="true"></i>
      {{ 'blogs.article.older_post' | t | link_to: blog.previous_article }}
    </div>    
    {%- endif -%}
    
    {%- if blog.next_article -%}
    <div class="btn-right">
      {{ 'blogs.article.newer_post' | t | link_to: blog.next_article }}
      <i class="fa fa-angle-right" aria-hidden="true"></i>
    </div>
    {%- endif -%}
    
  </div>
  {%- endif -%}  {%- if blog.comments_enabled? -%}  {%- comment -%}
  Just like our blog.liquid page, define how many comments should be on each page.
  {%- endcomment -%}  {%- if number_of_comments > 0 -%}
  <h2 class="title-comment">
    {{ 'blogs.comments.comments_with_count' | t: count: number_of_comments }}
  </h2>
  {%- endif -%}
  
  {%- paginate article.comments by 5 -%}  {%- comment -%}
  #comments is required, it is used as an anchor link by Shopify.
  {%- endcomment -%}
  
  <div id="comments">
    {%- if comment and comment.created_at -%}
    <div class="note form-success">
      {%- if blog.moderated? -%}
      <span {% if settings.enable_multilang %}data-translate="blogs.comments.success_moderated"{%endif%}>
        {{ 'blogs.comments.success_moderated' | t }}
      </span>
      {%- else -%}
      <span {% if settings.enable_multilang %}data-translate="blogs.comments.success"{%endif%}>
        {{ 'blogs.comments.success' | t }}
      </span>
      {%- endif -%}
    </div>
    {%- endif -%}    {%- if number_of_comments > 0 -%}
    <ul class="list-comments">
      {%- comment -%}If a comment was just submitted with no blank field, let's show it.{%- endcomment -%}
      {%- if comment and comment.created_at %}
      <li id="{{ comment.id }}" class="comment first{% if article.comments_count == 0 %} last{% endif %}">
        {% include 'comment' -%}
      </li>
      {%- endif -%}      {%- comment -%}
      Showing the rest of the comments.
      {%- endcomment -%}
      
      {%- for comment in article.comments -%}
      <li id="{{ comment.id }}" class="comment{% unless number_of_comments > article.comments_count %}{% if forloop.first %} first{% endif %}{% endunless %}{% if forloop.last %} last {% endif %}">
        {% include 'comment' %}
      </li>
      {%- endfor -%}
    </ul>    {%- if paginate.pages > 1 -%}
    {%- include 'pagination' -%}
    {%- endif -%}
    
    {%- endif -%}    {%- comment -%}
    Comment submission form
    {%- endcomment -%}    {%- form article -%}
    
    {%- comment -%}
    #add-comment-title is used simply as an anchor link
    {%- endcomment -%}
    
    <h3 class="title-comment" id="add-comment-title" {% if settings.enable_multilang %}data-translate="blogs.comments.title"{%endif%}>
      {{ 'blogs.comments.title' | t }}
    </h3>
    
    {%- include 'form-errors-custom' -%}
    
    <div class="comment-form">
      <div class="form-group">
        <label for="comment-author" {% if settings.enable_multilang %}data-translate="blogs.comments.name"{%endif%}>
          {{ 'blogs.comments.name' | t }}
        </label>
        <input {% if form.errors contains "author" %}class="error"{% endif %} type="text" name="comment[author]" id="comment-author" value="{{ form.author }}">
      </div>      <div class="form-group">
        <label for="comment-email" {% if settings.enable_multilang %}data-translate="blogs.comments.email"{%endif%}>
          {{ 'blogs.comments.email' | t }}
        </label>
        <input {% if form.errors contains "email" %}class="error"{% endif %} type="email" name="comment[email]" id="comment-email" value="{{ form.email }}">
      </div>
    </div>    <div class="message form-group">
      <label for="comment-body" {% if settings.enable_multilang %}data-translate="blogs.comments.message"{%endif%}>
        {{ 'blogs.comments.message' | t }}
      </label>
      <textarea rows="7" {% if form.errors contains "body" %}class="error"{% endif %} name="comment[body]" id="comment-body">
        {{ form.body }}
      </textarea>
    </div>    <div class="note" {% if settings.enable_multilang %}data-translate="blogs.comments.note"{%endif%}>
      {{ 'blogs.comments.note' | t }}
    </div>    <input type="submit" class="btn" value="{{ 'blogs.comments.post' | t }}">
    
    {%- if form.errors -%}
    <script>
      window.location.hash = '#add-comment-title';
    </script>
    {%- endif -%}    {%- if form.posted_successfully? -%}
    <script>
      window.location.hash = '#comments';
    </script>
    {%- endif -%}
    {%- endform -%}
  </div>
  {%- endpaginate -%}
  {%- endif -%}
</div>      </div>      
    </div>   
  </div>
</div>
{%- include 'article-schema-json' -%}
<!--GEM_FOOTER-->
{% capture GEM_FOOTER_SCRIPT %}
<script data-instant-track type="text/javascript">
	var pageLibs = [		'{{ 'gem-article-555454136394.js' | asset_url }}',	];
</script>
<script data-instant-track type="text/javascript">
	var GEMVENDOR = GEMVENDOR || (function(){		var _js = {};		return {			init: function(Args) {				_js = Args;			},			getLibs: function() {				return _js;			}		};	}());	GEMVENDOR.init(pageLibs);</script>
<script data-instant-track type="text/javascript" src="https://d1um8515vdn9kb.cloudfront.net/files/gempagev2.js?v=1.0" class="gf-script" defer></script>
{% endcapture %}
<!--GEM_FOOTER_END-->